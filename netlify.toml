[build]
  command = "hugo --gc --minify && npx -y pagefind --site public"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.165.0"

[context.production.environment]
  HUGO_ENV = "production"
